# Phase 2A 首轮固定对照实验报告

## 结论

本轮标记为 `invalid_failed_run`，不能作为改造前后 `cost per success` 基线，也不能据此宣称事件驱动方案已经节省某个比例的额度。

CodeBuddy 的 A、B 两个实现都通过了 6/6 测试，但均未创建任务要求的提交。实验脚本没有在 Codex 审查前验证这一完成门禁，并错误地对不存在的 `HEAD~1` 执行审查。原始输出与 usage JSONL 均保留。

此外，首轮 fixture 的 6 项测试漏掉了“首字符必须为 `a-z`”的三个反例；两个实现都错误接受以点、下划线或连字符开头的别名。测试通过不等于需求通过，这也是本轮无效的独立原因。

## 实测 Codex usage

| 变体 | 用途 | 会话 | input | cached input | output | reasoning output |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| A | 轮询 1 | A 轮询会话 | 31,014 | 26,112 | 156 | 31 |
| A | 轮询 2 | 同一会话 | 62,476 | 56,320 | 292 | 42 |
| A | 独立审核 | A 独立审核会话 | 48,365 | 42,240 | 768 | 324 |
| B | 独立审核 | B 独立审核会话 | 65,914 | 54,272 | 989 | 433 |

精确求和：A 为 3 次调用、141,855 input tokens；其中两次轮询为 93,490 input tokens。B 为 1 次调用、65,914 input tokens。这些数字只描述本次无效运行的实际消耗，不能当成成功任务成本。

当前桌面 Codex 会话的实验前后累计计数跨越了一个非连续计数边界：后值小于前值，因此 controller overhead 不可得，未做差值或分摊。

## H1-H5 判定

| 假设 | 判定 | 证据 |
| --- | --- | --- |
| H1 轮询占总消耗超过 50% | 无法判定 | 本轮无效；只能确认本轮 A 变体两次轮询实际用了 93,490 input tokens。无法外推历史总消耗占比。 |
| H2 同会话追加导致 input 单调膨胀 | 无法判定 | 同一轮询会话两次实测为 31,014 → 62,476，观察到增长；只有两个点，且未隔离上下文追加之外的因素，不能建立因果。 |
| H3 审核与 Codex 自实现同量级 | 无法判定 | 本轮没有 Codex 自实现对照。 |
| H4 属于总量配额，1h→2h 不代表省额度 | 无法判定 | 当前能观测 5 小时与周窗口 used percent，但拿不到账户内部计费权重和每次调用对窗口百分比的精确映射。时间不能替代 token 总量。 |
| H5 动态前缀使缓存失效 | 无法判定 | 有 cached input 实测字段，但没有服务端 prefix-cache key、命中原因或计费明细，不能从缓存数量反推出失效原因。 |

## 已修复的实验门禁

- 测试命令改为 fixture 根目录下的 `python -m unittest -v`。
- fixture 新增点、下划线、连字符开头的三个反例，并忽略测试生成的 `__pycache__`。
- CodeBuddy 完成必须同时满足：退出码 0、测试通过、创建新 commit、工作区干净。
- 只有完成门禁通过才允许进入 Codex 审查。
- 基线一致性改为比较两个初始 commit 的 tree hash，不再使用命令输出的真假值。
- 真实复跑不会自动发生；需再次取得用户确认。

## 下一次复跑的单变量约束

保持模型、fixture、prompt、max turns、超时和验收命令不变。只修复实验编排器的门禁与取数错误。若 CodeBuddy 再次未提交，实验应在实现阶段终止并保留现场，不调用 Codex 审核。

## 第二次运行记录

第二次运行同样没有形成有效对照。CodeBuddy A 已正确修改目标文件并通过 9/9 测试，但 PowerShell 7 将 `unittest -v` 写往 stderr 的正常进度转换为 `NativeCommandError`；全局 `ErrorActionPreference=Stop` 导致门禁提前终止。Codex 审核与 B 变体均未执行。

本次仅产生两次 A 轮询调用，实测分别为 31,001 与 62,429 input tokens，合计 93,430；cached input 分别为 26,112 与 52,224。它们属于第二次失败运行成本，不计入成功基线。

进一步的流程修正是把职责放回确定性脚本：CodeBuddy 负责改代码并跑测试；脚本验证“测试通过且只改预期文件”后创建 handoff commit；Codex 只审已经形成的 commit diff。这样不会把 Git 提交这一机械步骤继续交给模型碰运气，也不会放宽代码验收。

## 第三次运行记录

第三次运行首次完整走完 A/B，但仍判为无效：A、B 两个实现都把首字符规则写成 `[a-z0-9]`，错误接受数字开头。fixture 只覆盖了点、下划线和连字符开头，仍漏掉数字开头反例。A 审核识别出这个业务错误；B 审核没有识别出来。两次审核还都因为 Codex 隔离运行环境没有继承宿主 Python PATH 而无法重跑测试，最终均为 `REJECTED`。

本轮实测 A 三次调用共 157,988 input tokens，其中两次轮询为 93,606；B 一次审核为 80,156 input tokens。由于两个实现均违反完整规格且审核未通过，这些仍是失败运行成本，不是 `cost per success`。

由此新增两项确定性修正：fixture 加入 `1camera` 反例；脚本启动时解析宿主 Python 的绝对路径，并把同一路径明确传给 A/B 两个独立审核会话。它避免审核沙箱因 PATH 差异产生假阴性，但不会替审核放宽任何业务判断。

## 第四次暨最终运行记录

第四次运行的 A、B 实现均通过 10/10 宿主测试、变更范围门禁与确定性提交，两个 Codex 审核也都确认实现满足业务规则。但 Codex 的只读执行环境受应用控制策略限制，宿主 Python 绝对路径执行返回 Access denied，因此两个最终结论均为 `REJECTED`。脚本当时只检查 Codex CLI 退出码，没有解析最终审核结论，错误地以退出码 0 结束；复核原始 JSONL 后已将本轮严格判为端到端失败。

实测 usage：A 两次轮询分别为 31,043 与 62,612 input tokens，A 审核为 82,104，A 合计 175,759；B 审核为 65,457。A 的轮询本身消耗 93,655 input tokens，占本次 A 输入总量 53.29%。A 与 B 的审核输入并不相等，因此不能把二者总差额全部归因于轮询，也不能将这些失败链路数字称为 `cost per success`。

按照事先承诺，本阶段不再执行第五次真实实验。离线修正包括：审核结论必须以 `APPROVED` 开头才算通过；宿主脚本把测试命令、退出码、通过数和变更范围写成确定性 attestation，Codex 在只读环境审查 diff、测试代码与 attestation，不再尝试执行被系统策略禁止的宿主程序。下一次若需验证，将作为新的实验阶段单独确认，而不是继续 Phase 2A 重试。

## Attestation 离线验证

`scripts/probe_phase2a_attestation.ps1` 在临时 Git 仓库中执行，不调用 Codex、CodeBuddy 或网络。attestation v1 固定记录测试命令、退出码、通过数、基线 commit、变更文件以及源码和测试文件 SHA-256；验证器根据当前仓库状态和文件内容独立复算，而不是信任声明值。

实测结果：合法 attestation 被接受；伪造源码 hash、伪造测试 hash、失败退出码、增加越界文件四条负面路径全部被拒；测试退出码为 0、通过数为 10。临时仓库执行后删除，原始结构化结果保存在 `raw/phase2a-attestation-probe.json`。

该结果证明的是“宿主门禁产生的测试证明可以确定性校验”，不证明 Codex 一定会正确审核语义。后者若要测量，必须进入新的 Phase 2B，并重新取得真实模型调用确认。
