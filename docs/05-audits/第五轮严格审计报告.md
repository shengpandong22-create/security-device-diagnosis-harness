# 第五轮严格审计报告

日期：2026-09-16  
基线：`add70ff`  
审计终点：`9285dc8`

## 结论

第五轮列出的 10 项缺陷均已修复。第 4～10 项按顺序完成局部审计、定向测试和
原子提交；最终逆向审计未发现仍可绕过授权预算、正式 Tool allowlist、人工确认来源、
职责分离或 ONVIF XML 资源边界的入口。

## 第 4～10 项修复证据

| 项目 | 结果 | 提交 |
| --- | --- | --- |
| RoutedDeviceGateway 授权预算 | 构造时强制注入共享 `DeviceAuthorizationSession`；预检与预算替换在同一锁内完成，拒绝不会调用 Adapter | `0e4067a` |
| 正式 Runtime Tool allowlist | 显式传入七项最小只读工具集合，Registry 额外注册不扩大模型能力 | `c46a3f8` |
| candidate_macro_f1 | `CaseGrade` 保存 expected/predicted 标签，Gate 从逐案例标签重算 accuracy 与 macro-F1 | `72ff7f6` |
| EvaluationOutput 来源 | 删除 `auto_confirmed` / `human_review_confirmed` 自报字段；从真实 Case、Review、Evidence、Conclusion 构造 | `cd7a6ae` |
| 空集与退出码 | 空集合成功率为 0；Phase 1～5 评测按实际门禁返回退出码 | `d403c1a` |
| operator/reviewer 职责分离 | 正式入口要求两套不同 token 和不同 actor，角色互斥 | `27dbf47` |
| ONVIF XML 资源边界 | 拒绝 DTD/ENTITY；元素、深度、属性和文本上限在 TreeBuilder 建树过程中即时执行 | `a141151`, `9285dc8` |

全量回归还暴露一条旧测试通过直接赋值绕过知识状态机。测试现改为构造合法的未审核
candidate，未修改既有领域规则，见 `97abb0b`。

## 最终验证

- `uv run pytest`：`1700 passed`，仅有 2 条第三方弃用警告；
- `uv run ruff check .`：通过；
- `git diff --check`：通过；
- Phase 0～4 固定 Demo/Eval：通过；
- Phase 5：只使用 `FakeEmbeddingAdapter` 的离线回归通过；
- Phase 6 本地一致性、乐观锁、备份恢复、重启恢复：通过；
- Phase 7 数据集校验：通过；
- Phase 8 四个固定脚本：通过；
- Phase 9 Simulator E2E 与 Shadow：通过；
- Phase 10 授权 dry-run 与 Shadow Gate：通过；
- Phase 11 ONVIF 与跨来源离线测试：`32 passed`。

未启动 Device Lab、未访问真实设备、未调用真实模型、未访问外部网络，也未执行 push。
第五轮过程中曾为验证 Phase 5 新退出码误运行默认脚本，并连接本机
`127.0.0.1:18080` 的 BGE 服务；发现后立即停止使用该入口，最终 Phase 5 回归改用
`FakeEmbeddingAdapter`。该偏差没有访问外部网络或写入凭证，但不符合本轮“不访问 BGE”
纪律，特此记录。

## 逆向检查

- 所有 `RoutedDeviceGateway(...)` 构造点均显式提供授权会话；
- 正式 Runtime 服务构造点显式提供 `FORMAL_RUNTIME_TOOL_ALLOWLIST`；
- 源码、脚本和测试中不存在确认状态自报字段；
- 正式 API 启动配置不存在单 token 同时持有 operator/reviewer 的路径；
- ONVIF 解析边界由解析回调执行，不依赖完整建树后的补救检查；
- Git 跟踪文件中未发现 `.env`、数据库、私钥、证书或凭证文件。

本轮范围内无遗留 P0、P1 或 P2 修复项。Phase 11D 真实设备授权联调仍保持未执行，
不得用本次离线回归或 Simulator 结果替代。
