# Phase 7 Baselines

这里保存不含原始输入和密钥的评测基线。基线文件一经用于版本门禁不得原地覆盖；数据集、Prompt、模型或配置发生变化时必须建立新的基线系列。

`dev-1.0.0-deterministic-reference.json` 是 Phase 7C 的门禁协议基线，只证明确定性 Grader 与版本比较链路可重复运行，不是模型效果基线，也不能用于宣称生产准确率。真实模型基线将在 Phase 7D 独立命名和报告。
